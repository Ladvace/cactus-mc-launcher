# The launcher frontend needs no build-time env vars: it reads the backend URL
# from Rust at runtime (the `backend_base` command). All config — the backend
# URL (CACTUS_API_BASE) and other keys — lives in src-tauri/.env; see
# src-tauri/.env.example. Online features stay inert until a backend is set.
